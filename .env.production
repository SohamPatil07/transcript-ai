# Production environment variables
# These override .env during Netlify build (when NODE_ENV=production)
# Empty values tell the app to use relative URLs for API calls
VITE_API_BASE_URL=
VITE_EXTENSION_API_BASE_URL=
VITE_EXTENSION_APP_URL=
